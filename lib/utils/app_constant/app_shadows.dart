import 'package:flutter/material.dart';

class AppShadows {
  static final defaultShadows = [
    Shadow(
      color: Colors.black.withOpacity(0.25),
      offset: const Offset(0, 4),
      blurRadius: 4,
    )
  ];
}
